type Board {
  id: ID!
  name: String!
  created_at: String
}

type Query {
  getFromKanbanBoard: String
}

input CreateBoardInput {
  name: String!
}

type Mutation {
  createBoard(input: CreateBoardInput!): Board
}
